// Success

var pass;

pass = cloakDeepak('temp1234','h');

function chkpassword(pwd) {

    logDeepakFacetedValue(pwd);
    print('pwd:'+ getView(pwd));
};
cloakDeepak(chkpassword,'l');
chkpassword(pass);
